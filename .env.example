# Local environment config template for regime-rader.
# No hardcoded secrets/config currently exist in the codebase (checked
# filtered_hmm.py, model_selection.py, walk_forward.py, tests/) — there is
# no tuning_common.py in this project, so there are no keys to list yet.
# Copy this file to .env and populate it as env-driven config is added
# (e.g. data source API keys, DB paths).
